import { utilUserValidation } from './util-user-validation';

describe('utilUserValidation', () => {
  it('should work', () => {
    expect(utilUserValidation()).toEqual('util-user-validation');
  });
});
